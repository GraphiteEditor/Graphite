declare global {
	namespace Graphite {
		type Platform = "Windows" | "Mac" | "Linux" | "Web";
		type MenuType = "Popover" | "Dropdown" | "Dialog" | "Cursor";

		// interface Error {}
	}
}

export {};
